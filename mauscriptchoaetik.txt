-- ✅ MẪU CHUẨN CHO NGƯỜI MỚI | TÉO HUB 🍎
-- Giữ nguyên giao diện thật, chỉ thêm hướng dẫn 👈 ngay bên cạnh dòng cần sửa

repeat task.wait() until game:IsLoaded()

-- 🟢 Nút mở menu chính
local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ImageButton.Parent = ScreenGui
ImageButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.10615778, 0, 0.16217947, 0)
ImageButton.Size = UDim2.new(0, 40, 0, 40)
ImageButton.Draggable = true
ImageButton.Image = "http://www.roblox.com/asset/?id=76121098594454 "  -- 👈 đổi ID ảnh nút mở menu tại đây (ảnh roblox)

UICorner.CornerRadius = UDim.new(1, 10)
UICorner.Parent = ImageButton

ImageButton.MouseButton1Down:Connect(function()
	game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.End, false, game)
end)

-- 🟣 Giao diện Fluent
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
repeat wait() until game:IsLoaded()

-- 🪟 Cửa sổ chính
local Window = Fluent:CreateWindow({
	Title = "💀Mất Quyền Truy Cập",         -- 👈 đổi thành tên script của bạn
	SubTitle = "Xqhub",              -- 👈 đổi mô tả phụ
	TabWidth = 157,
	Size = UDim2.fromOffset(450, 300),
	Acrylic = true,
	Theme = "Amethyst",                   -- 👈 đổi màu chủ đạo: Amethyst, Dark, Aqua, Light,...
	MinimizeKey = Enum.KeyCode.End
})

-- 📑 Các tab (mục chính)
local Tabs = {
	Main0 = Window:AddTab({ Title = "một số kênh🤓" }),  -- 👈 đổi tên tab 1
	Main1 = Window:AddTab({ Title = "FARM Tiền $" }),  -- 👈 đổi tên tab 2
	Main2 = Window:AddTab({ Title = "FARM BOSS 🤖" }),     -- 👈 đổi tên tab 3
	Main3 = Window:AddTab({ Title = "PVP⚔️" }),        -- 👈 đổi tên tab 4
	Main4 = Window:AddTab({ Title = "SHOP Bảo Trì" }),    -- 👈 đổi tên tab 5
	Main5 = Window:AddTab({ Title = "FARM NPC" }) -- 👈 đổi tên tab 6
}

------------------------------------------------
-- TAB 1: Các kênh tiktok
------------------------------------------------
Tabs.Main0:AddButton({
	Title = "@xun.quyn15",              -- 👈 tên nút
	Description = "Nhấn vào để sao chép link tiktok ",-- 👈 mô tả nút
	Callback = function()
		setclipboard("https://www.tiktok.com/@xun.quyn15?_r=1&_t=ZS-91pwZ8A7SQa ")  -- 👈 link sao chép
	end
})

Tabs.Main0:AddButton({
	Title = "tiktok",
	Description = "fl tiktok để biết thêm nhiều script",
	Callback = function()
		setclipboard("https://www.tiktok.com/@xun.quyn15?_r=1&_t=ZS-91pwZ8A7SQa ")
	end
})

------------------------------------------------
-- TAB 2: Ví dụ thêm script
------------------------------------------------
Tabs.Main1:AddButton({
    Title = "ANTI AFK",
    Description = "Không bị kick AFK 👍",
    Callback = function()

        -- Thông báo hiện ngay khi bật
        pcall(function()
            game.StarterGui:SetCore("SendNotification", {
                Title = "ANTI AFK";
                Text = "Anti AFK đã bật!";
                Duration = 4;
            })
        end)

        -- Code chống AFK
        local vu = game:GetService("VirtualUser")
        local player = game.Players.LocalPlayer

        task.spawn(function()
            while true do
                player.Idled:Wait()
                vu:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
                task.wait(1)
                vu:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            end
        end)
    end
})

Tabs.Main2:AddButton({
	Title = "Farm boss",                             -- 👈 tên nút hiển thị
	Description = "nhấn vào để bật script ",-- 👈 mô tả
	Callback = function()
--// SETTINGS
local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hrp = char:WaitForChild("HumanoidRootPart")

local gui = player.PlayerGui:WaitForChild("BossMenu")

local kcBox = gui.Frame:WaitForChild("TextBox") -- Khoảng cách
local spBox = gui.Frame:WaitForChild("TextBox2") -- Tốc độ quay
local autoBtn = gui.Frame:WaitForChild("AutoBtn")
local noclipBtn = gui.Frame:WaitForChild("NoclipBtn")
local hpLabel = gui.Frame:WaitForChild("HP")

local autoFarm = false
local noclip = false

-----------------------
-- NOCLIP SYSTEM
-----------------------
game:GetService("RunService").Stepped:Connect(function()
    if noclip and char then
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanCollide = false
            end
        end
    end
end)

noclipBtn.MouseButton1Click:Connect(function()
    noclip = not noclip
    noclipBtn.Text = noclip and "TẮT: Noclip" or "BẬT: Noclip"
end)

-----------------------
-- FIND NEAREST BOSS
-----------------------
function getNearestBoss()
    local nearest = nil
    local dist = math.huge

    for _, mob in pairs(workspace:GetDescendants()) do
        if mob:FindFirstChild("Humanoid") and mob:FindFirstChild("HumanoidRootPart") then
            if mob.Humanoid.Health > 0 then
                local d = (mob.HumanoidRootPart.Position - hrp.Position).Magnitude
                if d < dist then
                    dist = d
                    nearest = mob
                end
            end
        end
    end

    return nearest
end

-----------------------
-- AUTO ATTACK FUNCTION
-----------------------
function attack()
    local tool = char:FindFirstChildOfClass("Tool")
    if tool and tool:FindFirstChild("Handle") then
        tool:Activate()
    end
end

-----------------------
-- AUTO FARM LOOP
-----------------------
autoBtn.MouseButton1Click:Connect(function()
    autoFarm = not autoFarm
    autoBtn.Text = autoFarm and "TẮT: Auto Đánh" or "BẬT: Auto Đánh"

    while autoFarm do
        task.wait(0.05)

        local boss = getNearestBoss()
        if boss == nil then
            hpLabel.Text = "Máu mục tiêu: Không tìm thấy!"
            continue
        end

        local bossHRP = boss:FindFirstChild("HumanoidRootPart")
        if not bossHRP then continue end

        hpLabel.Text = "Máu mục tiêu: " .. math.floor(boss.Humanoid.Health)

        local kc = tonumber(kcBox.Text) or 25
        local spin = tonumber(spBox.Text) or 2
        
        -- BAY TRÊN ĐẦU BOSS
        local abovePos = bossHRP.Position + Vector3.new(0, kc, 0)

        hrp.CFrame = CFrame.new(abovePos) * CFrame.Angles(0, math.rad(tick() * spin * 60), 0)

        -- TẤN CÔNG LIÊN TỤC
        attack()
    end
end)	
-- Tạo ScreenGui
local gui = Instance.new("ScreenGui", game.Players.LocalPlayer.PlayerGui)
gui.Name = "BossMenu"

-- Khung chính
local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 420, 0, 350)
main.Position = UDim2.new(0.5, -210, 0.5, -175)
main.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
main.BorderSizePixel = 0

-- Tiêu đề
local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1, 0, 0, 40)
title.Text = "BẬT: Đánh Boss"
title.TextColor3 = Color3.new(1,1,1)
title.BackgroundColor3 = Color3.fromRGB(20,20,20)
title.Font = Enum.Font.GothamBold
title.TextSize = 24

-- Ô khoảng cách
local kcLabel = Instance.new("TextLabel", main)
kcLabel.Position = UDim2.new(0, 20, 0, 60)
kcLabel.Size = UDim2.new(0, 150, 0, 35)
kcLabel.Text = "Khoảng cách:"
kcLabel.TextColor3 = Color3.new(1,1,1)
kcLabel.BackgroundTransparency = 1
kcLabel.Font = Enum.Font.Gotham
kcLabel.TextSize = 20

local kcBox = Instance.new("TextBox", main)
kcBox.Position = UDim2.new(0, 200, 0, 60)
kcBox.Size = UDim2.new(0, 80, 0, 35)
kcBox.Text = "30"
kcBox.Font = Enum.Font.Gotham
kcBox.TextSize = 20
kcBox.BackgroundColor3 = Color3.fromRGB(60,60,60)
kcBox.TextColor3 = Color3.new(1,1,1)

-- Ô tốc độ quay
local spLabel = Instance.new("TextLabel", main)
spLabel.Position = UDim2.new(0, 20, 0, 110)
spLabel.Size = UDim2.new(0, 150, 0, 35)
spLabel.Text = "Tốc độ quay:"
spLabel.TextColor3 = Color3.new(1,1,1)
spLabel.Font = Enum.Font.Gotham
spLabel.TextSize = 20
spLabel.BackgroundTransparency = 1

local spBox = Instance.new("TextBox", main)
spBox.Position = UDim2.new(0, 200, 0, 110)
spBox.Size = UDim2.new(0, 80, 0, 35)
spBox.Text = "2.3"
spBox.Font = Enum.Font.Gotham
spBox.TextSize = 20
spBox.BackgroundColor3 = Color3.fromRGB(60,60,60)
spBox.TextColor3 = Color3.new(1,1,1)

-- Màu mục tiêu
local hpLabel = Instance.new("TextLabel", main)
hpLabel.Position = UDim2.new(0, 20, 0, 155)
hpLabel.Size = UDim2.new(1, -40, 0, 30)
hpLabel.Text = "Máu mục tiêu: ..."
hpLabel.TextColor3 = Color3.fromRGB(255,50,50)
hpLabel.Font = Enum.Font.GothamBold
hpLabel.TextSize = 20
hpLabel.BackgroundTransparency = 1

-- Nút Auto Đánh
local autoBtn = Instance.new("TextButton", main)
autoBtn.Position = UDim2.new(0, 20, 0, 200)
autoBtn.Size = UDim2.new(1, -40, 0, 40)
autoBtn.Text = "BẬT: Auto Đánh"
autoBtn.Font = Enum.Font.GothamBold
autoBtn.TextColor3 = Color3.new(1,1,1)
autoBtn.TextSize = 22
autoBtn.BackgroundColor3 = Color3.fromRGB(70,70,70)

-- Nút Noclip
local noclipBtn = Instance.new("TextButton", main)
noclipBtn.Position = UDim2.new(0, 20, 0, 250)
noclipBtn.Size = UDim2.new(1, -40, 0, 40)
noclipBtn.Text = "BẬT: Noclip"
noclipBtn.Font = Enum.Font.GothamBold
noclipBtn.TextColor3 = Color3.new(1,1,1)
noclipBtn.TextSize = 22
noclipBtn.BackgroundColor3 = Color3.fromRGB(70,70,70)

-- Dòng trạng thái
local status = Instance.new("TextLabel", main)
status.Position = UDim2.new(0, 20, 0, 300)
status.Size = UDim2.new(1, -40, 0, 35)
status.Text = "TT:Mất Quyền Truy Cập"
status.TextColor3 = Color3.fromRGB(50,255,50)
status.Font = Enum.Font.GothamBold
status.TextSize = 22
status.BackgroundTransparency = 1
		local Settings = {
			JoinTeam = "Pirates";   -- 👈 team mặc định: Pirates hoặc Marines
			Translator = true;      -- 👈 bật dịch sang tiếng Việt nếu có
		}
		loadstring(game:HttpGet("https://raw.githubusercontent.com/AhmadV99/Speed-Hub-X/main/Speed%20Hub%20X.lua", true))() -- 👈 link script tải
	end
})

